const config = {
  plugins: ["@tailwindcss/postcss"],
  theme: {
    extend: {
      fontFamily: {
        amsterdamer: ["Amsterdamer Garamond", "serif"],
      },
    },
  },
};

export default config;
